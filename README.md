# DS-Lab-Task_-week10-
This Python file implements and compares the **Sequential Search** ($O(n)$) and **Binary Search** ($O(\log n)$) algorithms. It highlights the vast difference in efficiency between these methods, particularly on large lists, and emphasizes the requirement for **sorted data** when using Binary Search.
